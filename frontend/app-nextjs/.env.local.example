# Copy to .env.local. The Google Maps JS API key is exposed to the browser, so
# restrict it by HTTP referrer in the Google Cloud console.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Backend base URL for the /api/* rewrite proxy (defaults to localhost:8000).
BACKEND_URL=http://localhost:8000
